import Foundation

/// RevenueCat integration (Section 6 / Phase 5-6). Phase 0 stub.
/// The SDK is linked via SPM; wiring lands in the relevant phase.
final class RevenueCatService {
    static let shared = RevenueCatService()
    private init() {}
}
