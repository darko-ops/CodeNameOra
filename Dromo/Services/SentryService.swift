import Foundation

/// Sentry integration (Section 6 / Phase 5-6). Phase 0 stub.
/// The SDK is linked via SPM; wiring lands in the relevant phase.
final class SentryService {
    static let shared = SentryService()
    private init() {}
}
