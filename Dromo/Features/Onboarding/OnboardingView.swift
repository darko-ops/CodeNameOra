import SwiftUI

/// Welcome + value prop + music platform choice (Phase 5). Phase 0 stub.
struct OnboardingView: View {
    var body: some View { Text("Onboarding").foregroundColor(.oraTextSecondary) }
}
