import SwiftUI

/// Appears on tap during an active session (Phase 3). Phase 0 stub.
struct PauseResumeSheet: View {
    var body: some View { Text("Pause / Resume").foregroundColor(.oraTextSecondary) }
}
