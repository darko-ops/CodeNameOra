import Foundation

/// Binds DromoCore engine output to the active-session UI (Phase 3). Phase 0 stub.
@MainActor final class ActiveSessionViewModel: ObservableObject {
    // TODO(Phase 3): subscribe to PaceEngine / MusicSequencer output.
}
