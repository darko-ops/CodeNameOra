import SwiftUI

/// Pace-zone badge (Phase 3). Phase 0 stub.
struct ZoneBadgeView: View {
    var body: some View {
        Text("STEADY")
            .font(.system(size: 10, weight: .medium))
            .foregroundColor(.zoneSteady)
    }
}
